# 知径 Plan B

先遵守上级工作台入口，再阅读本目录 README 与 `docs/PLAN-B.md`。

- 这是 li 明确要求从知径复制的独立方向，不是正本的临时工作树。
- 所有新功能、配置与产物只修改本目录。不得修改同级 `个性化学习平台/` 或原站点。
- 保留现有问卷、六形式学习、来源对照与小猫；新方向是独立的生活应用对话页，不替换学习页。
- 使用独立本地端口 3100 / 4430、`data-plan-b/`、`.env.plan-b` 与 Plan B 会话 Cookie。
- 不导入旧用户数据或密钥，不把原 GitHub 设成推送目标。
- 2026-09-18，li 已授权 B 版常驻部署到原服务器的独立 `/zhijing-b/` 路由，目录 `/home/ubuntu/apps/zhijing-plan-b`，服务 `zhijing-plan-b-web` / `zhijing-plan-b-api`。不得覆盖 A 版目录、服务、路由或数据；共享 Nginx 仅增加 B 版独立 include，并验证后平滑 reload。
- li 已授权独立公开仓库 `lizx-lzx/zhijing-plan-b`；仅向该仓库推送 B 版源码与公开素材，禁止提交本地数据、密钥、私人对话和飞书原文快照。公开源码不等于部署 B 版网站。
- 问卷走演示流程：套用预设规则模板并保存所选偏好后，直接进入「待启集」，不调用模型、不增加 Skill 确认页。读法笺仍可主动编辑。
- 知行当前也只做预设对话演示，不接 AI。唯一产品入口在猫猫的对话框里；独立知行页保存场景、对话和用户确认的知识记录，不额外弹出读完邀请，不修改长期学习 Skill。
- `README.知径原版.md` 与旧开发记录描述原项目；原版线上地址及部署命令不是本副本的执行指令。
- 不虚构知乎人物、经历、引文与学习效果。实时检索尚未接入；讨论中的功能不写成已实现。
